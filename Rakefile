# frozen_string_literal: true

require "bundler/gem_tasks"
require "rspec/core/rake_task"

require File.join(File.dirname(__FILE__),"lib","release","gem")

RSpec::Core::RakeTask.new(:spec)

task default: :spec
